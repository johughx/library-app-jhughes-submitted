<!-- NAVIGATION -->
<div class="floatLeft">
	<div id="nav">
    <ul>
      <%= content_tag(:li) do  %>
  			<%= link_to 'Librarian View', list_users_path %>
      <% end %>

      <%= content_tag(:li) do  %>
  			<%= link_to 'Customer View', user_path(Customer.first, view: 'customer') %>
      <% end %>
    </ul>
  </div>
</div>
  
